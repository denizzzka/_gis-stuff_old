Это пока непригодно к использованию
(кроме runtime.d, где содержатся процедуры разбора кодирования полей)

Тут я хочу провернуть такую штуку: сделать plugin для protoc на D, общающийся
с protoc через stdin/stdout по протоколу plugin.proto. И еще хотелось бы чтобы
полученный при компиляции код использовал реализации функций из runtime.d, а не
библиотеку libprotobuf.

При этом получится что парсер синтаксиса .proto используется гугловский, но
результирующий код целиком написан на D.
